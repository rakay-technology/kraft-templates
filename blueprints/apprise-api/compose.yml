services:
  apprise-api:
    image: linuxserver/apprise-api:version-v1.5.4
    restart: unless-stopped
    expose:
      - 8000
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=UTC
    volumes:
      - config:/config
volumes:
  config: {}
