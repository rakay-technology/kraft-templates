[variables]
main_domain = "${domain}"
root_email = "${email}"
root_password = "${password:32}"

[[routes]]
service = "openobserve"
port = 5080
host = "${main_domain}"

[env]
ZO_ROOT_USER_EMAIL = "${root_email}"
ZO_ROOT_USER_PASSWORD = "${root_password}"
ZO_DATA_DIR = "/data"
