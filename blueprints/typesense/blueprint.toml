[variables]
main_domain = "${domain}"
api_key = "${password:16}"
enable_cors = "true"

[[routes]]
service = "typesense"
port = 8108
host = "${main_domain}"

[env]
TYPESENSE_API_KEY = "${api_key}"
TYPESENSE_ENABLE_CORS = "${enable_cors}"
