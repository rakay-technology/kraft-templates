[variables]
main_domain = "${domain}"
encryption_key = "${base64:32}"

[[routes]]
service = "pocket-id"
port = 1411
host = "${main_domain}"

[env]
APP_URL = "${main_domain}"
ENCRYPTION_KEY = "${encryption_key}"
TRUST_PROXY = "true"
