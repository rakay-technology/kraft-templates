[variables]
main_domain = "${domain}"
movies_path = "/path/to/movies"
tv_path = "/path/to/tv"

[[routes]]
service = "bazarr"
port = 6767
host = "${main_domain}"

[env]
MOVIES_PATH = "${movies_path}"
TV_PATH = "${tv_path}"
