services:
  postiz-app:
    image: ghcr.io/gitroomhq/postiz-app:v1.45.1-arm64
    restart: always
    environment:
      MAIN_URL: https://${main_domain}
      FRONTEND_URL: https://${main_domain}
      NEXT_PUBLIC_BACKEND_URL: https://${main_domain}/api
      JWT_SECRET: ${jwt_secret}
      DATABASE_URL: postgresql://${db_user}:${db_password}@postiz-postgres:5432/${db_name}
      REDIS_URL: redis://postiz-redis:6379
      BACKEND_INTERNAL_URL: http://127.0.0.1:3000
      TEMPORAL_ADDRESS: temporal:7233
      IS_GENERAL: "true"
      DISABLE_REGISTRATION: "false"
      RUN_CRON: "true"
      STORAGE_PROVIDER: local
      UPLOAD_DIRECTORY: /uploads
      NEXT_PUBLIC_UPLOAD_DIRECTORY: /uploads
      FACEBOOK_APP_ID: ""
      FACEBOOK_APP_SECRET: ""
      INSTAGRAM_APP_ID: ""
      INSTAGRAM_APP_SECRET: ""
      LINKEDIN_CLIENT_ID: ""
      LINKEDIN_CLIENT_SECRET: ""
      X_API_KEY: ""
      X_API_SECRET: ""
      YOUTUBE_CLIENT_ID: ""
      YOUTUBE_CLIENT_SECRET: ""
      TIKTOK_CLIENT_ID: ""
      TIKTOK_CLIENT_SECRET: ""
    volumes:
      - postiz-config:/config/
      - postiz-uploads:/uploads/
    depends_on:
      - postiz-postgres
      - postiz-redis
      - temporal
    expose:
      - 5000
  postiz-postgres:
    image: postgres:17-alpine
    restart: always
    environment:
      POSTGRES_PASSWORD: ${db_password}
      POSTGRES_USER: ${db_user}
      POSTGRES_DB: ${db_name}
    volumes:
      - postgres-volume:/var/lib/postgresql/data
    healthcheck:
      test:
        - CMD-SHELL
        - pg_isready -U ${DB_USER} -d ${DB_NAME}
      interval: 10s
      timeout: 3s
      retries: 3
      start_period: 10s
  postiz-redis:
    image: redis:7.2
    restart: always
    healthcheck:
      test:
        - CMD
        - redis-cli
        - ping
      interval: 10s
      timeout: 3s
      retries: 3
    volumes:
      - postiz-redis-data:/data
  temporal-elasticsearch:
    image: elasticsearch:7.17.27
    restart: always
    environment:
      - cluster.routing.allocation.disk.threshold_enabled=true
      - cluster.routing.allocation.disk.watermark.low=512mb
      - cluster.routing.allocation.disk.watermark.high=256mb
      - cluster.routing.allocation.disk.watermark.flood_stage=128mb
      - discovery.type=single-node
      - ES_JAVA_OPTS=-Xms256m -Xmx256m
      - xpack.security.enabled=false
    volumes:
      - temporal-es-data:/usr/share/elasticsearch/data
  temporal-postgresql:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_PASSWORD: ${temporal_password}
      POSTGRES_USER: ${temporal_user}
    volumes:
      - temporal-pg-data:/var/lib/postgresql/data
    healthcheck:
      test:
        - CMD-SHELL
        - pg_isready -U ${TEMPORAL_USER}
      interval: 10s
      timeout: 5s
      retries: 5
  temporal:
    image: temporalio/auto-setup:1.28.1
    restart: always
    depends_on:
      - temporal-postgresql
      - temporal-elasticsearch
    environment:
      - DB=postgres12
      - DB_PORT=5432
      - POSTGRES_USER=${temporal_user}
      - POSTGRES_PWD=${temporal_password}
      - POSTGRES_SEEDS=temporal-postgresql
      - ENABLE_ES=true
      - ES_SEEDS=temporal-elasticsearch
      - ES_VERSION=v7
      - TEMPORAL_NAMESPACE=default
    healthcheck:
      test:
        - CMD-SHELL
        - tctl --address $$(hostname -i):7233 --namespace default workflow list
          > /dev/null 2>&1
      interval: 15s
      timeout: 10s
      retries: 30
      start_period: 120s
    volumes: []
volumes:
  postgres-volume: null
  postiz-redis-data: null
  postiz-config: null
  postiz-uploads: null
  temporal-es-data: null
  temporal-pg-data: null
