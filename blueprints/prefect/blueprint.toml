[variables]
main_domain = "${domain}"
postgres_password = "${password:32}"

[[routes]]
service = "prefect"
port = 4200
host = "${main_domain}"

[env]
PREFECT_DOMAIN = "${main_domain}"
POSTGRES_PASSWORD = "${postgres_password}"
