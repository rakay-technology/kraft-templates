services:
  ipfs:
    image: ipfs/kubo:v0.43.0
    restart: unless-stopped
    environment:
      - IPFS_PROFILE=server
    volumes:
      - ipfs_data:/data/ipfs
      - ipfs_staging:/export
    expose:
      - 4001
      - 5001
      - 8080
volumes:
  ipfs_data: {}
  ipfs_staging: {}
