services:
  keydb:
    image: eqalpha/keydb:x86_64_v6.3.4
    restart: unless-stopped
    command: keydb-server /etc/keydb/keydb.conf --server-threads 2 --requirepass
      ${KEYDB_PASSWORD}
    volumes:
      - keydb-data:/data
volumes:
  keydb-data: {}
